language: python

jobs:
  include:
  - language: python
    python: 3.6
    install:
    - pip install -r backend/requirements.txt
    script:
    - pytest -v backend/*_tests.py
  - language: node_js
    node_js: 12
    before_install:
    - cd frontend
    install:
    - npm install
    script:
    - npm run build
